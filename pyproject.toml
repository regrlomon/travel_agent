[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "travel-agent"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["agent*", "tools*", "worker*", "api*"]
